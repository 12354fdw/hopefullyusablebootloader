a custom bootloader lol

config file format
```
instant boot? Y or N
kernel path
initrd path (if none NONE)
Shell path (if none NONE)
```

keybinds

Enter = boot kernel

S = Shutdown

D = Shell

F = Firmware setting
